# FRONTEND.md - Designali Hub Frontend Documentation

## 📋 Índice
1. [Overview do Projeto](#overview-do-projeto)
2. [Funcionalidades Implementadas](#funcionalidades-implementadas)
3. [O Que Precisa Ser Feito](#o-que-precisa-ser-feito)
4. [Sugestões de Melhorias](#sugestões-de-melhorias)
5. [Arquitetura e Padrões](#arquitetura-e-padrões)
6. [Guia de Desenvolvimento](#guia-de-desenvolvimento)

---

## Overview do Projeto

**Designali Hub** é uma aplicação React moderna de gerenciamento de produtividade e aprendizado com integração Supabase e IA (Google Gemini). A aplicação é um SPA (Single Page Application) que funciona como um hub centralizado para gerenciar ferramentas de design, vídeos educacionais, notas, cursos e recursos de aprendizado.

### Stack Tecnológico

| Camada | Tecnologia | Versão |
|--------|-----------|--------|
| **Frontend** | React + TypeScript | 19.2.0 / 5.8.2 |
| **Build** | Vite | 6.2.0 |
| **Styling** | Tailwind CSS (CDN) | Latest |
| **Charts** | Recharts | 3.4.1 |
| **Backend** | Supabase (PostgreSQL) | 2.81.1 |
| **AI** | Google Gemini API | 1.29.0 |
| **Notifications** | react-hot-toast | 2.6.0 |

### Ambiente Local

```bash
# Instalar dependências
npm install

# Iniciar servidor de desenvolvimento (porta 3000)
npm run dev

# Build para produção
npm run build

# Preview da build
npm preview
```

---

## Funcionalidades Implementadas

### ✅ 1. **Dashboard** (`pages/Dashboard.tsx`)

**Status:** ✔️ Funcional

**Features Implementadas:**
- Exibição de estatísticas em cards (Tools, Videos, Notas, Cursos, Recursos)
- Gráfico de pizza mostrando distribuição de ferramentas por categoria
- Gráfico de barras mostrando progresso de cursos
- Botões de ação rápida para navegar para outras seções
- Real-time subscription do Supabase (atualização automática de stats)
- Navegação inteligente: clique nos cards vai direto para a página

**Dados Exibidos:**
- Total de ferramentas, vídeos, notas, cursos completados, recursos
- Breakdown de ferramentas por categoria (gráfico pizza)
- Progresso de cursos (gráfico barras)

**Melhorias Necessárias:**
- [ ] Adicionar mais visualizações de dados (timeline, trending tools, etc.)
- [ ] Cache de dados para reduzir queries

---

### ✅ 2. **Ferramentas (Tools)** (`pages/Ferramentas.tsx`)

**Status:** ✔️ Funcional (com integração IA)

**Features Implementadas:**
- ✅ CRUD completo (Create, Read, Update, Delete)
- ✅ Grid view e List view
- ✅ Busca por nome da ferramenta
- ✅ Filtro por categoria
- ✅ Filtro por favoritos
- ✅ Toggle de favoritos
- ✅ Exibição de ícones/imagens
- ✅ Links abertos em nova aba
- ✅ **IA Integrada**: Sugestões de ferramentas via Google Gemini API
- ✅ Sincronização real-time com Supabase

**Modal de Adição/Edição:**
- Nome da ferramenta (obrigatório)
- URL (obrigatório)
- Categoria (obrigatório)
- Descrição
- URL da imagem
- Save/Cancel buttons

**Componentes Reutilizáveis:**
- `ToolCard` - Vista em grid
- `ToolListItem` - Vista em lista
- Modal genérico para forms

**Fluxo de IA (Gemini):**
1. User clica em "Sugerir Ferramentas"
2. Envia prompt ao Gemini API
3. Recebe sugestões estruturadas (JSON)
4. Exibe em modal com opção de adicionar

**Melhorias Necessárias:**
- [ ] Validação de URL mais rigorosa
- [ ] Upload de imagens ao invés de apenas URL
- [ ] Bulk import de ferramentas
- [ ] Organização por tags
- [ ] Histórico de uso de ferramentas

---

### ✅ 3. **Vídeos** (`pages/Videos.tsx`)

**Status:** ✔️ Funcional

**Features Implementadas:**
- ✅ CRUD completo
- ✅ Grid view e List view
- ✅ Busca por título
- ✅ Filtro por plataforma (YouTube, Vimeo, Other)
- ✅ Filtro por favoritos
- ✅ Toggle de favoritos
- ✅ Geração automática de thumbnail do YouTube
- ✅ Links abertos em nova aba
- ✅ Real-time sync com Supabase

**Modal de Adição/Edição:**
- Título (obrigatório)
- URL (obrigatório)
- Plataforma (YouTube, Vimeo, Other)
- Canal/Fonte
- Descrição
- Duração (segundos)
- Thumbnail (autogenerated para YouTube)

**Detecção de Plataforma:**
- Automática por URL
- YouTube → extrai thumbnail e ID de vídeo
- Suporta URLs de:
  - youtube.com/watch?v=
  - youtu.be/
  - vimeo.com/
  - URLs customizadas

**Melhorias Necessárias:**
- [ ] Preview inline de vídeos (player embed)
- [ ] Watch list com progresso
- [ ] Rating/Reviews
- [ ] Categorização por tópico
- [ ] Playlist de vídeos
- [ ] Sincronização com YouTube channel

---

### ✅ 4. **Notas** (`pages/Notas.tsx`)

**Status:** ✔️ Funcional (com Markdown)

**Features Implementadas:**
- ✅ CRUD completo
- ✅ Grid view e List view
- ✅ Busca por título
- ✅ Filtro por tags
- ✅ Filtro por favoritos
- ✅ Toggle de favoritos
- ✅ Suporte a Markdown (via marked.js CDN)
- ✅ Sistema de tags (multi-select)
- ✅ Timestamp de atualização
- ✅ Real-time sync

**Modal de Adição/Edição:**
- Título (obrigatório)
- Conteúdo (textarea com Markdown support)
- Tags (input com autocomplete)
- Preview em tempo real do Markdown

**Suporte Markdown:**
- Headers (H1-H6)
- Bold, Italic, Code
- Listas (ordered, unordered)
- Links e imagens
- Código com syntax highlighting
- Tabelas

**Melhorias Necessárias:**
- [ ] Rich text editor ao invés de apenas textarea
- [ ] Pintura de sintaxe (syntax highlight) no preview
- [ ] Nested tags/categories
- [ ] Backup automático (auto-save)
- [ ] Histórico de versões
- [ ] Export em PDF
- [ ] Colaboração em tempo real
- [ ] Voice-to-text
- [ ] Templates de notas

---

### ✅ 5. **Estudo (Courses)** (`pages/Estudo.tsx`)

**Status:** ✔️ Funcional

**Features Implementadas:**
- ✅ CRUD completo
- ✅ Tabs (Cursos / Tutoriais)
- ✅ Progresso de cursos (0-100%)
- ✅ Status de curso (Not Started, In Progress, Completed)
- ✅ Filtro por plataforma
- ✅ Filtro por status
- ✅ Filtro por favoritos
- ✅ Real-time sync
- ✅ Barra de progresso visual

**Tab de Cursos:**
- Título, Plataforma, Progress (%), Status
- Instrutor
- URL para acessar
- Marcação de favorito

**Tab de Tutoriais:**
- Título, URL, Fonte
- Links diretos

**Campos Armazenados:**
- courses: title, platform, progress, status, description, instructor, url
- tutorials: title, url, source

**Melhorias Necessárias:**
- [ ] Estimativa de tempo para completar
- [ ] Certificados/Badges
- [ ] Integração com plataformas (Udemy, Coursera, Skillshare)
- [ ] Reminders/Notifications de progresso
- [ ] Analytics de aprendizado
- [ ] Mapa de conhecimento
- [ ] Quiz/Assessment
- [ ] Recomendações baseadas em progresso

---

### ✅ 6. **Recursos (Resources)** (`pages/Recursos.tsx`)

**Status:** ✔️ Funcional

**Features Implementadas:**
- ✅ CRUD completo
- ✅ Grid view e List view
- ✅ Busca por título
- ✅ Filtro por tipo (Article, Book, Podcast, Other)
- ✅ Filtro por favoritos
- ✅ Toggle de favoritos
- ✅ Links abertos em nova aba
- ✅ Real-time sync

**Tipos de Recursos:**
- Article (artigos online)
- Book (e-books, referências)
- Podcast (áudio, episódios)
- Other (genérico)

**Modal de Adição/Edição:**
- Título (obrigatório)
- URL
- Tipo (Article, Book, Podcast, Other)
- Descrição
- Autor
- Data de adição

**Melhorias Necessárias:**
- [ ] Integração com APIs (Medium, Dev.to, Podcasts)
- [ ] RSS Feed reader
- [ ] Anotações em recursos
- [ ] Leitura estimada
- [ ] Recomendações
- [ ] Compartilhamento
- [ ] Integração com Readwise/Pocket

---

### ✅ 7. **LoginPage** (`pages/LoginPage.tsx`)

**Status:** ✔️ Estrutura Pronta (sem auth real)

**Features Implementadas:**
- ✅ Toggle Login/Signup UI
- ✅ Form com email e password
- ✅ Design responsivo com gradiente
- ✅ Validação básica de campos

**Status Atual:**
- ⚠️ **Não há autenticação real implementada**
- Demo: qualquer email/password faz login
- Sem persistência de sessão

**Melhorias Necessárias:**
- [ ] **Implementar autenticação via Supabase Auth**
- [ ] OAuth (Google, GitHub)
- [ ] Email verification
- [ ] Password reset
- [ ] Session persistence
- [ ] Proteção de rotas

---

### ✅ 8. **Componentes Reutilizáveis**

**Status:** ✔️ Implementados

#### `components/Header.tsx`
- Título dinâmico da página
- Search bar (busca ainda não integrada globalmente)
- Ícone de notificações (placeholder)
- Menu de usuário (placeholder)

#### `components/Sidebar.tsx`
- Navegação com 7 pages
- Ícones visuais
- Destaque da página ativa
- Responsive design

#### `components/Modal.tsx`
- Componente genérico modal
- Overlay escuro
- Close button
- Customizável com children

#### `components/Card.tsx`
- Wrapper para cards
- Hover effects
- Borda e shadow
- Responsivo

#### `components/ContentPageLayout.tsx`
- Layout padrão para páginas de conteúdo
- Search/Filter bar
- View mode toggle (Grid/List)
- Botão de adicionar item
- Skeleton para loading

#### `components/Icons.tsx`
- 20+ SVG icons
- Ícones para: Dashboard, Tools, Videos, Notes, Study, Resources, Settings, etc.
- Customizável com className

---

## O Que Precisa Ser Feito

### 🔴 CRÍTICO (Bloqueadores)

#### 1. **Autenticação Real** (LoginPage)
**Prioridade:** CRÍTICA
**Esforço:** Alto
**Descrição:** A autenticação atual é dummy. Precisa implementar autenticação real com Supabase.

**Tarefas:**
- [ ] Integrar `supabase.auth.signUp()` para cadastro
- [ ] Integrar `supabase.auth.signInWithPassword()` para login
- [ ] Armazenar token em localStorage/sessionStorage
- [ ] Verificar token ao carregar app
- [ ] Redirecionar não autenticados para login
- [ ] Implementar logout
- [ ] Proteção de rotas (route guards)
- [ ] OAuth integrations (Google, GitHub)
- [ ] Email verification flow
- [ ] Password reset functionality
- [ ] Session persistence

**Componentes Afetados:**
- `App.tsx` - verificação de autenticação
- `LoginPage.tsx` - forms de login/signup
- Todas as páginas - proteção de acesso

---

#### 2. **Row Level Security (RLS) Policies**
**Prioridade:** CRÍTICA
**Esforço:** Médio
**Descrição:** Database tem RLS ativado mas com políticas abertas (allow all). Precisa restringir por usuário.

**Tarefas:**
- [ ] Adicionar `user_id` a todas as tabelas
- [ ] Criar RLS policies para SELECT (ler dados do usuário)
- [ ] Criar RLS policies para INSERT (criar apenas seu próprio dado)
- [ ] Criar RLS policies para UPDATE (editar apenas seu próprio dado)
- [ ] Criar RLS policies para DELETE (deletar apenas seu próprio dado)
- [ ] Testar todas as políticas

**Estrutura de Dados:**
```sql
-- Adicionar user_id a todas as tabelas
ALTER TABLE tools ADD COLUMN user_id UUID REFERENCES auth.users(id);
ALTER TABLE videos ADD COLUMN user_id UUID REFERENCES auth.users(id);
ALTER TABLE notes ADD COLUMN user_id UUID REFERENCES auth.users(id);
ALTER TABLE courses ADD COLUMN user_id UUID REFERENCES auth.users(id);
ALTER TABLE resources ADD COLUMN user_id UUID REFERENCES auth.users(id);
```

---

#### 3. **Real-time Subscriptions Bug Fixes**
**Prioridade:** ALTA
**Esforço:** Médio
**Descrição:** Algumas páginas têm problemas com real-time updates do Supabase.

**Tarefas:**
- [ ] Verificar se subscriptions são criadas corretamente
- [ ] Corrigir memory leaks (unsubscribe no unmount)
- [ ] Adicionar error handling para subscriptions
- [ ] Testar com múltiplas abas abertas
- [ ] Implementar retry logic

**Padrão Correto:**
```typescript
useEffect(() => {
  const channel = supabase
    .channel('changes')
    .on('postgres_changes',
      { event: '*', schema: 'public', table: 'table_name' },
      payload => handleChange(payload)
    )
    .subscribe();

  return () => {
    supabase.removeChannel(channel);
  };
}, []);
```

---

### 🟠 ALTO (Importantes)

#### 4. **Validação de Formulários**
**Prioridade:** ALTA
**Esforço:** Médio
**Descrição:** Forms não têm validação adequada.

**Tarefas:**
- [ ] Validar campos obrigatórios antes de submit
- [ ] Validar formato de email
- [ ] Validar URLs (regex ou library)
- [ ] Validar comprimento de campos
- [ ] Mostrar erros ao user
- [ ] Desabilitar submit enquanto há erros

**Biblioteca Sugerida:** `zod` ou `react-hook-form`

---

#### 5. **Error Handling Global**
**Prioridade:** ALTA
**Esforço:** Médio
**Descrição:** Não há tratamento de erros global ou por página.

**Tarefas:**
- [ ] Adicionar try-catch em todas as operações Supabase
- [ ] Mostrar toast de erro ao user
- [ ] Logging de erros (console + backend)
- [ ] Fallback UI para erros
- [ ] Retry logic para operações de rede
- [ ] Rate limiting handling

---

#### 6. **Loading States e Skeletons**
**Prioridade:** ALTA
**Esforço:** Médio
**Descrição:** Páginas não têm indicadores de loading.

**Tarefas:**
- [ ] Adicionar loading state em cada página
- [ ] Criar skeleton components
- [ ] Mostrar spinner durante operações async
- [ ] Desabilitar buttons enquanto carregando
- [ ] Timeout handling

---

#### 7. **Melhorar Performance**
**Prioridade:** ALTA
**Esforço:** Alto
**Descrição:** Aplicação pode ficar lenta com muitos dados.

**Tarefas:**
- [ ] Implementar paginação (ao invés de carregar tudo)
- [ ] Lazy loading de imagens
- [ ] Memoization de componentes pesados
- [ ] Code splitting (lazy routes)
- [ ] Otimizar queries Supabase
- [ ] Cache local (localStorage)
- [ ] Debounce em search
- [ ] Virtual scrolling para listas grandes

---

#### 8. **Configurações de Usuário** (Configuracoes page)
**Prioridade:** ALTA
**Esforço:** Médio
**Descrição:** Página existe mas está vazia (placeholder).

**Tarefas:**
- [ ] Criar formulário de perfil do usuário
- [ ] Foto de perfil
- [ ] Email e nome
- [ ] Preferências (tema, idioma)
- [ ] Notificações
- [ ] Privacidade
- [ ] Integração com OAuth
- [ ] Export de dados
- [ ] Delete account

---

### 🟡 MÉDIO (Nice-to-Have)

#### 9. **Melhorias de UX**
**Prioridade:** MÉDIA
**Esforço:** Médio

**Tarefas:**
- [ ] Drag & drop para reordenar items
- [ ] Undo/Redo
- [ ] Keyboard shortcuts
- [ ] Dark mode
- [ ] Responsive mobile (mobile-first design)
- [ ] Accessibility (WCAG 2.1 AA)
- [ ] Temas customizáveis
- [ ] Tooltips
- [ ] Onboarding tutorial

---

#### 10. **Features Avançadas**
**Prioridade:** MÉDIA
**Esforço:** Alto

**Tarefas:**
- [ ] Compartilhamento de ferramentas/recursos
- [ ] Colaboração em tempo real
- [ ] Comentários em items
- [ ] Sistema de tags global
- [ ] Busca avançada (full-text search)
- [ ] Filtros salvos
- [ ] Estatísticas avançadas
- [ ] Exportação em múltiplos formatos (CSV, JSON, PDF)
- [ ] Importação em batch
- [ ] Integração com calendário (estudar por agenda)

---

#### 11. **Melhorias de IA**
**Prioridade:** MÉDIA
**Esforço:** Médio

**Tarefas:**
- [ ] Sugestões de recursos baseadas em histórico
- [ ] Auto-tagging de notas
- [ ] Resumo automático de recursos
- [ ] Recomendação de cursos
- [ ] Q&A assistant baseado em suas notas
- [ ] Análise de gap de conhecimento

---

#### 12. **Testing**
**Prioridade:** MÉDIA
**Esforço:** Alto

**Tarefas:**
- [ ] Setup de test framework (Vitest)
- [ ] Unit tests para componentes
- [ ] Integration tests para flows
- [ ] E2E tests (Playwright/Cypress)
- [ ] Coverage > 70%

---

### 🟢 BAIXO (Future)

#### 13. **Features de Gamification**
**Prioridade:** BAIXA
**Esforço:** Alto
- Pontos/Badges para atividades
- Streaks de aprendizado
- Leaderboard
- Achievements

#### 14. **Mobile App**
**Prioridade:** BAIXA
**Esforço:** Muito Alto
- React Native app
- Sincronização offline-first
- Push notifications

#### 15. **AI Features Avançadas**
**Prioridade:** BAIXA
**Esforço:** Alto
- Chat com Claude/GPT sobre notas
- Geração de study plans
- Análise de aprendizado personalizado

---

## Sugestões de Melhorias

### 🏗️ Arquitetura

#### 1. **Estrutura de Pastas Melhorada**
**Atual:**
```
├── components/
├── pages/
├── services/
├── types.ts
```

**Sugerido:**
```
src/
├── components/
│   ├── common/          # Components reutilizáveis
│   ├── layouts/         # Layout components
│   └── features/        # Feature-specific components
├── pages/
├── services/
│   ├── supabase.ts
│   └── api/            # API calls abstraídas
├── hooks/              # Custom React hooks
├── context/            # React Context (auth, theme, etc)
├── utils/              # Helper functions
├── types/              # Type definitions
├── constants/          # Constants e configs
└── styles/             # Global styles se necessário
```

#### 2. **Context API para State Global**
Atualmente não há state global. Sugerido:

```typescript
// contexts/AuthContext.tsx
export const AuthContext = createContext<AuthContextType | null>(null);

// contexts/ThemeContext.tsx
export const ThemeContext = createContext<ThemeContextType | null>(null);

// contexts/NotificationContext.tsx
export const NotificationContext = createContext<NotificationContextType | null>(null);
```

#### 3. **Custom Hooks**
Criar hooks para operações comuns:

```typescript
// hooks/useTools.ts
export function useTools() {
  const [tools, setTools] = useState<Tool[]>([]);
  const [loading, setLoading] = useState(false);

  const fetchTools = async () => { /* ... */ };
  const addTool = async (tool: Tool) => { /* ... */ };
  const deleteTool = async (id: string) => { /* ... */ };

  return { tools, loading, fetchTools, addTool, deleteTool };
}

// hooks/useAuth.ts
// hooks/useLocalStorage.ts
// hooks/usePagination.ts
// hooks/useInfiniteScroll.ts
```

#### 4. **API Service Layer**
Abstrair chamadas Supabase:

```typescript
// services/api/toolsApi.ts
export const toolsApi = {
  getAll: () => supabase.from('tools').select('*'),
  getById: (id: string) => supabase.from('tools').select('*').eq('id', id),
  create: (tool: Tool) => supabase.from('tools').insert(tool),
  update: (id: string, data: Partial<Tool>) =>
    supabase.from('tools').update(data).eq('id', id),
  delete: (id: string) => supabase.from('tools').delete().eq('id', id),
};
```

---

### 🎨 Design e UX

#### 1. **Design System / Component Library**
- Criar Storybook para documentar componentes
- Paleta de cores bem definida
- Tipografia consistente
- Spacing system (8px grid)
- Icon library centralizada (SVG sprite ou package)

#### 2. **Responsive Design**
Aplicação não é mobile-friendly. Sugerido:
- Mobile-first approach
- Breakpoints: sm (640px), md (768px), lg (1024px), xl (1280px)
- Drawer/Hamburger menu em mobile
- Layout adaptativo para tablets

#### 3. **Tema Escuro (Dark Mode)**
- Context para alternar temas
- Armazenar preferência em localStorage
- Support para `prefers-color-scheme`
- Definir cores dark mode

#### 4. **Acessibilidade (a11y)**
- ARIA labels
- Keyboard navigation
- Focus management
- Color contrast compliance (WCAG AA)
- Screen reader testing

---

### 🔧 Código e Performance

#### 1. **Otimização de Renderização**
```typescript
// Usar memo para componentes que não mudam frequentemente
export const ToolCard = memo(({ tool }: Props) => {...});

// Usar useMemo para computações pesadas
const filteredTools = useMemo(() =>
  tools.filter(t => t.category === selectedCategory),
  [tools, selectedCategory]
);

// Usar useCallback para event handlers
const handleDelete = useCallback((id: string) => {
  deleteItem(id);
}, [deleteItem]);
```

#### 2. **Code Splitting**
```typescript
// pages/Ferramentas.tsx
const Ferramentas = lazy(() => import('./Ferramentas'));

// Em App.tsx
<Suspense fallback={<Loading />}>
  <Ferramentas />
</Suspense>
```

#### 3. **Bundle Size**
- Remover Firebase imports não usados
- Tree-shaking adequado
- Minify/Compress
- Lazy load libraries pesadas
- Usar dynamic imports

#### 4. **Caching Strategy**
```typescript
// Service worker para cache offline
// LocalStorage para dados frequentes
// SessionStorage para sessão
// Cache API para assets estáticos
```

---

### 📊 Analytics e Observabilidade

#### 1. **Error Logging**
- Integrar Sentry ou similar
- Capturar erros em produção
- Stack traces de erros
- User session tracking

#### 2. **Performance Monitoring**
- Core Web Vitals (LCP, FID, CLS)
- Tempo de carregamento de página
- Métrica de interatividade
- Monitor de performance do Supabase

#### 3. **Analytics**
- Eventos de user (página visitada, item criado, etc)
- Funil de conversão (login → usar app)
- Feature usage tracking
- User engagement

---

### 🧪 Testing

#### 1. **Unit Tests**
```typescript
// __tests__/components/Card.test.tsx
describe('Card Component', () => {
  it('should render children', () => {
    render(<Card><p>Test</p></Card>);
    expect(screen.getByText('Test')).toBeInTheDocument();
  });
});
```

#### 2. **Integration Tests**
- Testar fluxos completos (add/edit/delete)
- Testar interação com Supabase
- Testar real-time updates

#### 3. **E2E Tests**
- Testar login flow
- Testar navegação
- Testar operações CRUD
- Testar responsividade

---

### 📚 Documentação

#### 1. **README Melhorado**
- Setup local
- Deploy instructions
- Estrutura de pastas
- Contributing guidelines
- Troubleshooting

#### 2. **Storybook**
- Documentar todos os componentes
- Exemplos de uso
- Props documentation
- Visual regression testing

#### 3. **API Documentation**
- Documentar hooks customizados
- Services
- Types
- Constants

---

### 🚀 Deployment e DevOps

#### 1. **CI/CD Pipeline**
```yaml
# .github/workflows/test.yml
- Rodar tests em cada PR
- Build check
- Lint check
- Deploy automático em produção
```

#### 2. **Environment Management**
- `.env.local` (development)
- `.env.test` (testing)
- `.env.production` (production)
- Secrets gerenciados (não no repo)

#### 3. **Vercel/Netlify Deployment**
- Auto-deploy de main branch
- Preview deploys para PRs
- Environment variables configuradas
- Domain/SSL setup

---

### 🔐 Segurança

#### 1. **Input Validation**
- Validar em frontend (UX)
- Validar em backend (Supabase) - crítico
- Sanitize HTML em notes

#### 2. **CORS e CSRF**
- Configurar CORS no Supabase
- CSRF tokens se necessário

#### 3. **Secrets Management**
- Nunca committar `.env.local`
- Usar environment variables
- Rotate API keys regularmente

#### 4. **XSS Prevention**
- React escapa HTML por padrão
- Cuidado com dangerouslySetInnerHTML
- Sanitize markdown rendering

#### 5. **SQL Injection**
- Usar Supabase client (prepared statements)
- RLS policies
- Validação de entrada

---

## Arquitetura e Padrões

### Fluxo de Dados

```
User Interaction → Component Event Handler
  ↓
Supabase Service (CRUD)
  ↓
Database / Real-time Subscription
  ↓
State Update (useState)
  ↓
Component Re-render
  ↓
UI Update
```

### Padrão de CRUD

```typescript
const [items, setItems] = useState<Item[]>([]);
const [loading, setLoading] = useState(false);

// Read
const fetchItems = async () => {
  setLoading(true);
  const { data } = await supabase.from('items').select('*');
  setItems(data || []);
  setLoading(false);
};

// Create
const addItem = async (item: Item) => {
  const { data } = await supabase.from('items').insert([item]);
  setItems([...items, data[0]]);
};

// Update
const updateItem = async (id: string, updates: Partial<Item>) => {
  await supabase.from('items').update(updates).eq('id', id);
  setItems(items.map(i => i.id === id ? {...i, ...updates} : i));
};

// Delete
const deleteItem = async (id: string) => {
  await supabase.from('items').delete().eq('id', id);
  setItems(items.filter(i => i.id !== id));
};
```

### Padrão de Real-time

```typescript
useEffect(() => {
  const channel = supabase
    .channel('db-changes')
    .on(
      'postgres_changes',
      {
        event: '*',
        schema: 'public',
        table: 'items'
      },
      (payload) => {
        if (payload.eventType === 'INSERT') {
          setItems(prev => [...prev, payload.new]);
        } else if (payload.eventType === 'UPDATE') {
          setItems(prev => prev.map(i =>
            i.id === payload.new.id ? payload.new : i
          ));
        } else if (payload.eventType === 'DELETE') {
          setItems(prev => prev.filter(i => i.id !== payload.old.id));
        }
      }
    )
    .subscribe();

  return () => {
    supabase.removeChannel(channel);
  };
}, []);
```

### Padrão de Modal

```typescript
const [isOpen, setIsOpen] = useState(false);
const [editingItem, setEditingItem] = useState<Item | null>(null);

const handleEdit = (item: Item) => {
  setEditingItem(item);
  setIsOpen(true);
};

const handleClose = () => {
  setIsOpen(false);
  setEditingItem(null);
};

const handleSave = async (data: Item) => {
  if (editingItem) {
    await updateItem(editingItem.id, data);
  } else {
    await addItem(data);
  }
  handleClose();
};

return (
  <>
    <Modal isOpen={isOpen} onClose={handleClose}>
      <Form item={editingItem} onSave={handleSave} />
    </Modal>
  </>
);
```

---

## Guia de Desenvolvimento

### Adicionando uma Nova Feature

#### 1. Nova Página
```typescript
// 1. Criar arquivo em pages/NovaPagina.tsx
const NovaPagina: React.FC = () => {
  return <div>Nova Página</div>;
};
export default NovaPagina;

// 2. Adicionar à enum em types.ts
export enum Page {
  // ... existing
  NovaPagina = 'NovaPagina',
}

// 3. Importar em App.tsx
import NovaPagina from './pages/NovaPagina';

// 4. Adicionar case em renderPage()
case Page.NovaPagina:
  return <NovaPagina />;

// 5. Adicionar ao title mapping
[Page.NovaPagina]: 'Nova Página',

// 6. Adicionar em Sidebar.tsx
<SidebarLink
  icon={IconComponent}
  label="Nova Página"
  onClick={() => setActivePage(Page.NovaPagina)}
/>
```

#### 2. Novo Componente
```typescript
// components/MeuComponente.tsx
interface MeuComponenteProps {
  title: string;
  onClick: () => void;
}

const MeuComponente: React.FC<MeuComponenteProps> = ({ title, onClick }) => {
  return <div onClick={onClick}>{title}</div>;
};

export default MeuComponente;
```

#### 3. Novo Hook
```typescript
// hooks/useMeuHook.ts
export function useMeuHook() {
  const [state, setState] = useState(null);

  const fetchData = useCallback(async () => {
    // implementation
  }, []);

  useEffect(() => {
    fetchData();
  }, [fetchData]);

  return { state, fetchData };
}
```

### Criando Tipos TypeScript

```typescript
// types.ts
export interface MeuItem extends Item {
  nome: string;
  descricao: string;
  categoria: string;
  tags: string[];
}

export type MeuItemForm = Omit<MeuItem, 'id' | 'isFavorite'>;
```

### Integrando com Supabase

```typescript
// services/meuServico.ts
export const meuServico = {
  // Get all
  async getAll() {
    const { data, error } = await supabase
      .from('meu_item')
      .select('*');
    if (error) throw error;
    return data;
  },

  // Get by ID
  async getById(id: string) {
    const { data, error } = await supabase
      .from('meu_item')
      .select('*')
      .eq('id', id)
      .single();
    if (error) throw error;
    return data;
  },

  // Create
  async create(item: MeuItemForm) {
    const { data, error } = await supabase
      .from('meu_item')
      .insert([item])
      .select()
      .single();
    if (error) throw error;
    return data;
  },
};
```

### Tratamento de Erros

```typescript
try {
  const result = await meuServico.getAll();
  setData(result);
} catch (error) {
  console.error('Erro ao carregar dados:', error);
  toast.error('Falha ao carregar dados. Tente novamente.');
}
```

### Usando Toast Notifications

```typescript
import toast from 'react-hot-toast';

// Success
toast.success('Item adicionado com sucesso!');

// Error
toast.error('Erro ao adicionar item');

// Loading
const t = toast.loading('Carregando...');
setTimeout(() => {
  toast.success('Pronto!', { id: t });
}, 2000);

// Custom
toast.custom((t) => (
  <div className="bg-white p-4">Custom</div>
));
```

---

## Resumo de Prioridades

### 🔴 CRÍTICO - Fazer AGORA
1. Autenticação real (Supabase Auth)
2. RLS Policies por usuário
3. Real-time subscription fixes
4. Error handling global

### 🟠 ALTO - Fazer em Sprint Próximo
5. Validação de formulários
6. Loading states
7. Performance optimization
8. Configurações de usuário

### 🟡 MÉDIO - Fazer em 1-2 meses
9. Dark mode
10. Mobile responsiveness
11. Acessibilidade
12. Melhorias de IA

### 🟢 BAIXO - Fazer depois
13. Testing framework
14. Advanced features
15. Mobile app
16. Gamification

---

## Conclusão

**Designali Hub** é uma aplicação bem estruturada com boas bases. As principais prioridades são:

1. ✅ **Autenticação real** - sem isso, a app não é segura
2. ✅ **RLS policies** - proteção de dados crítica
3. ✅ **Error handling** - melhor experiência do usuário
4. ✅ **Performance** - aplicação responsiva

Uma vez resolvidos esses itens críticos, a aplicação estará pronta para uso. As melhorias posteriores focam em experiência do usuário e features avançadas.

**Tempo estimado:**
- Crítico: 2-3 semanas
- Alto: 3-4 semanas
- Médio: 4-6 semanas
- Total: 2-3 meses para MVP polido

---

**Documento gerado:** 12 de Novembro de 2024
